This is an example website with tech and tables.

Frontend and Backend setup is in shell script format so we can run it easily.

Once we create servers for frontend and backend we can run the respective scripts. 

